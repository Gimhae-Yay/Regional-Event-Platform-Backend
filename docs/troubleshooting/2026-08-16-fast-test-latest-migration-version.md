# V43 추가 후 초기 스키마 테스트 실패

## 요약

| 항목 | 내용 |
| --- | --- |
| 상태 | 해결 |
| 영향 | Issue #874 구현 검증 중 `fastTest` 2,039개 중 1개 실패 |
| 최초 확인 시각·시간대 | 2026-08-16 22:52 KST |
| 관련 요구사항·이슈 | Issue #874, 수정본 무효화 사유 `CONTENT_WITHDRAWN` 허용 |
| revision·브랜치 | `f53f5059`, `feature/874-approve-content-withdrawal` |
| 환경·프로필 | Windows, Java 21.0.6, Gradle `fastTest` |

## 기대 결과와 실제 결과

### 기대 결과

신규 V43을 포함한 전체 Flyway 마이그레이션과 `fastTest`가 통과해야 한다.

### 실제 결과

`InitialP0SchemaMigrationTest.빈_데이터베이스에_현재_스키마를_생성한다`가 적용 버전 목록의 마지막 값을
V42로 기대해 실패했다. 실제 빈 스키마에는 V43까지 정상 적용됐다.

## 재현 절차

### 선행 조건

- `V43__allow_content_withdrawn_revision_invalidation.sql`이 존재한다.
- `InitialP0SchemaMigrationTest`의 기대 버전 목록은 V42에서 끝난다.

### 명령·요청·입력

1. `.\gradlew.bat fastTest --no-daemon`

### 재현 결과

- 실행 횟수: 1
- 성공 횟수: 0
- 실패 횟수: 1
- 종료 코드·HTTP 상태: 1

## 수집한 증거

- 전체 결과: 2,039 tests completed, 1 failed
- 실패 위치: `InitialP0SchemaMigrationTest.java:139`
- 실제 Flyway 최신 마이그레이션: V43
- 테스트 기대 목록의 마지막 버전: `"42"`

## 조사 타임라인

| 시각 | 구분 | 가설 또는 작업 | 예측·검증 | 결과 | 판정 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-16 22:52 KST | 관찰 | `fastTest` 실패 확인 | 실패 테스트와 검증식을 특정한다 | 초기 스키마 적용 버전 목록 비교에서 실패 | 채택 |
| 2026-08-16 22:53 KST | 가설 | V43 추가 후 고정 기대 목록이 갱신되지 않았다 | 기대 목록은 V42, 실제 이력은 V43까지 존재한다 | 코드와 실행 결과가 예측과 일치 | 채택 |
| 2026-08-16 22:55 KST | 변경·검증 | 기대 목록에 V43 추가 후 단일 테스트 재실행 | 원래 실패 테스트가 통과한다 | 성공 | 채택 |
| 2026-08-16 23:01 KST | 회귀 검증 | 전체 `fastTest` 재실행 | 모든 빠른 테스트가 통과한다 | 2,039개 통과 | 채택 |

## 가설과 검증

### 가설 1: 최신 마이그레이션 기대 목록 누락

- 근거: V43 파일은 존재하지만 테스트 기대 목록은 V42에서 끝난다.
- 참일 때의 예측: 기대 목록에 V43을 추가하면 동일 테스트가 통과한다.
- 반증 조건: V43 추가 뒤에도 다른 스키마 검증에서 실패한다.
- 검증 방법: 기대 목록만 V43까지 확장하고 실패 테스트를 단독 재실행한다.
- 결과: 단일 실패 테스트와 전체 `fastTest`가 모두 통과했다.
- 판정: 채택

## 근본 원인

- 촉발 조건: Issue #874 범위의 순방향 V43 마이그레이션 추가
- 결함이 있는 코드·설정·데이터·계약: 초기 스키마 테스트의 고정된 Flyway 버전 기대 목록
- 증상으로 이어진 메커니즘: 실제 적용 목록에는 `43`이 추가됐지만 `containsExactly` 기대 목록은 `42`에서 끝나 비교가 실패했다.
- 기존 방어가 막지 못한 이유: 새 마이그레이션 추가 시 고정 기대 목록을 함께 갱신해야 하는 구조다.
- 결론의 증거: 실패 위치, V43 파일, 기대 목록 마지막 값이 직접 일치한다.

## 해결 또는 완화

- 선택한 방법: 초기 스키마 테스트의 적용 버전 기대 목록에 `"43"`을 추가한다.
- 변경 파일: `src/test/java/io/regionevent/regioneventbackend/global/config/InitialP0SchemaMigrationTest.java`
- 정책·계약 변경 여부: 없음

## Before/After 검증

| 검증 항목 | Before | After | 판정 |
| --- | --- | --- | --- |
| 원래 재현 절차 | 2,039개 중 1개 실패 | 2,039개 통과 | 해결 |

## 회귀 테스트

| 테스트·명령 | 결과 | 비고 |
| --- | --- | --- |
| `.\gradlew.bat fastTest --no-daemon` | 2,039개 중 1개 실패 | 수정 전 재현 |
| `.\gradlew.bat test --no-daemon --tests '*InitialP0SchemaMigrationTest'` | 통과 | 최소 회귀 검증 |
| `.\gradlew.bat fastTest --no-daemon` | 2,039개 통과 | 전체 빠른 테스트 회귀 검증 |

## 재발 방지와 문서 반영

새 Flyway 버전 추가 시 초기 스키마 적용 버전 검증도 함께 갱신한다.

## 잔여 위험과 후속 작업

Issue #876 범위의 다중 연결 경합과 저장 단계별 MySQL 롤백 주입은 이번 조사 범위가 아니다.

## 관련 자료

- Issue #874 구현 인계 요약
