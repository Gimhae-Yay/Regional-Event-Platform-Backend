# AGENTS.md

## 역할

이 파일은 저장소 전체의 작업 진입점이다. 상세 규칙을 중복해서 적지 않고 기준 문서로 연결한다.
더 가까운 하위 `AGENTS.md`가 있으면 해당 문서를 우선한다.

## 작업 원칙

1. **문서 먼저, 코드 나중**: 설계 결정은 구현 전에 `docs/`에 기록한다. 코드는 확정된 문서를 구현한다.
2. **한국어 사용**: 모든 소통과 문서는 한국어로 작성한다. 코드 식별자와 커밋 메시지 제목에만 영어를 허용한다.
3. **최소 조건**: 요구사항에 없는 기능은 만들지 않는다. 향후 확장 가능성은 구현하지 않고 문서의 `확장 지점`에만 기록한다.
4. **선택지 비교 후 결정**: 설계 방향이 갈리면 추천안을 첫 번째에 둔 장단점 비교표를 제시한다. 사용자가 선택한 안을 정책으로 확정하고 문서에 반영한다.

## 필수 절차

1. 요청 범위와 변경 대상을 확인한다.
2. **모든 구현과 리뷰 전에 `docs/code-convention.md` 전체를 읽고 반드시 적용한다.** 컨벤션을 바꿀 때는 이 파일이 아니라 `docs/code-convention.md`를 수정한다.
3. 아래 표의 관련 기준 문서와 작업 범위에 대응하는 `docs/local-stamp-platform-prd.md` 항목 또는 연결된 이슈를 읽는다.
4. 설계 결정이 필요하면 선택지를 비교해 사용자 결정을 받은 뒤 `docs/`에 먼저 기록한다.
5. 구현 단계는 전용 스킬 없이 진행한다. 연결된 GitHub 이슈의 완료 조건을 구현 계약으로 삼고, docs/code-convention.md와 이 문서의 작업 원칙을 벗어나지 않는다.
6. 코드, 테스트, migration, 설정과 의존성의 현재 상태를 확인한 뒤 문서에 확정된 범위만 변경한다.
7. `docs/code-convention.md`의 검증 및 완료 보고 규칙을 따른다.

## 개발 워크플로

아래 순서를 지킨다. 스킬이 있는 단계는 `.agents/skills/`의 해당 스킬을 단일 기준으로 삼고,
구현 단계는 스킬 없이 이 문서와 `docs/code-convention.md`를 단일 기준으로 삼는다.
구현은 이슈별 작업(feature) 브랜치에서 진행하고 main·dev에 직접 커밋하지 않는다.

| 순서 | 단계    | 기준            | 규칙                                        |
|----|-------|---------------|-------------------------------------------|
| 1  | 이슈 생성 | `issue-create` 스킬 | PRD를 기준으로 기반 이슈를 만들고 API마다 이슈를 하나씩 만든다. |
| 2  | 구현    | 스킬 없음 · `AGENTS.md` + `docs/code-convention.md` | 연결된 이슈의 완료 조건만 구현한다. 전용 스킬을 찾지 않고 이 문서의 작업 원칙·필수 절차와 코드 컨벤션을 적용한다. |
| 3  | 커밋    | `commit` 스킬   | 구현된 내용을 `type: 한국어 설명` 형식으로 커밋한다.      |
| 4  | PR 생성 | `pr-create` 스킬 | 기능 PR은 `feature`에서 `dev`로 생성한다.        |
| 5  | PR 리뷰 | `pr-review` 스킬 | 리뷰는 읽기 전용으로 수행하고, 사용자가 게시를 요청하면 COMMENT로 해당 PR에 남긴다.            |

## 문서 라우팅

| 확인할 내용                                        | 기준 문서                                                         |
|-----------------------------------------------|---------------------------------------------------------------|
| 확정된 기술 결정·설계 근거             | `docs/adr/`                                                   |
| 기술 스택·설계 원칙             | `docs/local-stamp-platform-tech-stack.md`                                                   |
| 제품 범위, 보안·동시성·정합성·Redis 정책             | `docs/local-stamp-platform-prd.md`                            |
| API 요청·응답, validation, status, 에러 코드          | `docs/api-specification.md`                                   |
| 엔티티, 테이블, 관계, 컬럼, DB 제약·인덱스                   | `docs/erd.md`                                                 |
| 도메인 규칙, 패키지 구조, 구현, 코드 스타일, 트랜잭션, 테스트, Git·PR | `docs/code-convention.md`                                     |
| 실제 버전·의존성·설정                                  | `build.gradle`, `settings.gradle`, `src/**/application*.yaml` |

충돌 시 담당 영역의 기준 문서를 따르며, 제품·API·DB·성능·컨벤션 순으로 판단한다.
코드가 문서와 다르다는 이유만으로 코드를 기준 삼지 않는다.
모호하거나 기준 문서가 없으면 추측하지 말고 차이와 필요한 결정을 밝힌다.